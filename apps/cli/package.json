{
  "name": "@trackboard/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "trackboard": "./dist/src/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.5.0"
  }
}
